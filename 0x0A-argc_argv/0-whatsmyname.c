#include <stdio.h>
/**
 * main - entry point
 * @argc: argument count
 * @argv: array for arg info
 * Return: 0
 */
int main (int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s", argv[0]);
}
